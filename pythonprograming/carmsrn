
print("Enter the Number")
number = int(input())
temp = int(number)
Sum = 0
while(temp != 0):
    rem  = temp % 10
    Sum  = Sum + (rem * rem * rem) 
    temp = temp / 10 
if(number == Sum):
    print ("no")
else:
    print ("yes")
